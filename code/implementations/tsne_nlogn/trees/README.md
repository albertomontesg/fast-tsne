The n log n algorithm needs two kinds of trees (a vantage point tree (vptree) and a quadtree (or higher dimensional equavalents - sptree)).

For now those are taken from the van Maaten impelmentation.